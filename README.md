Requires a soundfont and pipewire routed to the output for sound
Kinda rough
Works only on X11
The controls should mostly be obvious from the code, but the keys for the mapping are either note keys or buttons on the midi controller. If they are keys, the number is the semitone from C0 (or I think so, I made this like 6 months ago.)
